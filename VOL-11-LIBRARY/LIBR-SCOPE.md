# VOL-11-LIBRARY — Scope (UID LIBR)

Reference documentation, templates, and guides.

This volume is for reading material — not active application code. Active
runtime utilities live in `VOL-06-APPLICATIONS/APPS-Utils` instead.
