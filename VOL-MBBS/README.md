# VOL-MBBS

Machin3-Bas3 Broadcasting Station — social/external presence links (Reddit, etc).

Distinct from `VOL-04-PUBLIC` (UID PUBL), which holds served static assets.
This volume is for tracking/linking out to accounts and channels, not assets.
